import styled from 'styled-components';

export const PaginationWrapper = styled.div`
    position: absolute; 
    bottom: 32px; 
    left: 0;
    right: 0;
    display: flex;
    justify-content: center;
`